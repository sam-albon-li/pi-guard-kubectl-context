{
  "name": "pi-guard-kubectl-context",
  "version": "1.0.0",
  "description": "Pi extension that blocks kubectl commands unless --context targets an allowed cluster",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "node --test test/extension.test.ts",
    "test:e2e": "bash test/e2e.sh"
  },
  "keywords": [
    "pi-package"
  ],
  "files": [
    "index.ts"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
